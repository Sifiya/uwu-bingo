export type BingoCell = {
  text: string;
  textSize: number;
  index: number;
};

export type BingoColors = {
  background: string;
  title: string;
  cell: string;
  text: string;
  border: string;
};
